# Web-specific overrides. Most patterns now live in the repo-root .gitignore.
Thumbs.db

# Local marketing-site working notes (web-only, kept out of the public repo).
# The app's own CLAUDE.md and docs are intentionally tracked — see app/CLAUDE.md.
CLAUDE.md
docs/

